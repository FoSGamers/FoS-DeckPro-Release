# PhaseSynth Ultra+ Configuration
# Generated by System Initializer

# Project settings
project:
  name: "PhaseSynth Ultra+"
  version: "1.0.0"
  description: "Universal AI-Driven IDE Auto-Healing System"

# APTPT settings
aptpt:
  default_gain: 0.16
  default_noise: 0.005
  convergence_threshold: 0.03
  max_iterations: 1000

# HCE settings
hce:
  phase_stability_threshold: 0.95
  harmonic_coherence_threshold: 0.8
  resonance_quality_threshold: 0.7

# REI settings
rei:
  universal_xi: 7.24e-12
  invariance_threshold: 0.95
  proportionality_ratio: 1.0
  recursive_depth: 3
  recursive_depth_limit: 10

# System settings
system:
  database_path: "phase_synth.db"
  log_level: "INFO"
  max_history_size: 1000
