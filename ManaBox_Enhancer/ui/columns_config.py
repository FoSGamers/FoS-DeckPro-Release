# Centralized default columns for card tables in ManaBox Enhancer
# Used by both main_window.py and break_builder.py

DEFAULT_COLUMNS = [
    "Name", "Set name", "Set code", "Collector number", "Rarity",
    "Condition", "Foil", "Language", "Purchase price", "Whatnot price"
] 